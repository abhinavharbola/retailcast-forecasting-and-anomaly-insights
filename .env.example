# LLM providers: narrative.py tries these in the order set by llm.provider_priority in config.yaml
NIM_API_KEY =
GROQ_API_KEY =
GEMINI_API_KEY =

# Supabase stores forecast runs, generated reports, anomaly flags
SUPABASE_URL =
SUPABASE_KEY =

# DagsHub / MLflow, only needed if the dashboard queries past training runs
DAGSHUB_TOKEN =
DAGSHUB_REPO =
